### Form di base

Creare una pagina index.html con i normali headers

Crea un modulo (<form>) con un campo di testo (<input type="text">), un'area di testo (<textarea>), e un pulsante di invio (<button>).